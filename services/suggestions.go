package services


import(

)


func (s *Service) SaveToSearchHistory(userid, req string) {
	//save user's search request
}

func (s *Service) SaveToWatchHistory(userid, req string) {
	//save user's get requests 
}

func (s *Service) Suggestions(userid string) {
	//suggest lots and auctions to user by user's watch and search history
}